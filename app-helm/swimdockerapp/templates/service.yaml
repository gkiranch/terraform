apiVersion: v1
kind: Service
metadata:
  name: {{ include "swimdockerapp.fullname" . }}
  labels:
    {{- include "swimdockerapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "swimdockerapp.selectorLabels" . | nindent 4 }}
