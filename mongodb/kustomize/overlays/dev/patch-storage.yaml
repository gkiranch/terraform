apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: mongo-db
spec:
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        resources:
          requests:
            storage: 10Gi
