# inventory.aws_ec2.yaml
plugin: amazon.aws.aws_ec2
regions: [us-east-1]
filters:
  tag:aws:autoscaling:groupName: "*node-group*"
compose:
  ansible_host: private_ip_address
keyed_groups:
  - key: tags.Role